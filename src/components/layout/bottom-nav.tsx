'use client'; import { useEffect, useMemo, useState } from 'react'; import { usePathname, useRouter } from 'next/navigation'; import { Home, LayoutDashboard, ShieldCheck, User, Vault, ScrollText } from 'lucide-react'; import { useI18n } from '@/i18n/provider'; type Item = { href: string; label: string; icon: React.ComponentType<{ className?: string }> }; type Role = 'pending' | 'user' | 'approver' | 'admin' | 'super_admin'; const ELEVATED_ROLES: Role[] = ['approver', 'admin', 'super_admin']; export function BottomNav({ admin = false }: { admin?: boolean }) { const pathname = usePathname(); const router = useRouter(); const { t, locale } = useI18n(); const [canSeeRequests, setCanSeeRequests] = useState(() => { if (admin) return false; if (typeof window === 'undefined') return false; const cachedRole = window.sessionStorage.getItem('pv_role'); return Boolean(cachedRole && ELEVATED_ROLES.includes(cachedRole as Role)); }); useEffect(() => { if (admin) return; const controller = new AbortController(); void fetch('/api/profile/me', { method: 'GET', cache: 'no-store', signal: controller.signal, }).then(async (res) => { const body = (await res.json().catch(() => ({}))) as { role?: string }; const role = String(body.role ?? 'user') as Role; window.sessionStorage.setItem('pv_role', role); setCanSeeRequests(ELEVATED_ROLES.includes(role)); }).catch(() => {}); return () => controller.abort(); }, [admin]); const userItems: Item[] = useMemo(() => { const items: Item[] = [{ href: '/home', label: t('nav.home'), icon: Home }, { href: '/vault', label: t('nav.vault'), icon: Vault }]; if (canSeeRequests) { items.push({ href: '/requests', label: locale === 'th' ? 'คำขอสมัครใช้งาน' : 'Signup Requests', icon: ShieldCheck }); } items.push({ href: '/settings', label: t('nav.settings'), icon: User }); return items; }, [canSeeRequests, locale, t]); const adminItems: Item[] = useMemo(() => [{ href: '/dashboard', label: t('nav.home'), icon: LayoutDashboard }, { href: '/approvals', label: t('nav.approvals'), icon: ShieldCheck }, { href: '/users', label: t('nav.users'), icon: User }, { href: '/audit-logs', label: t('nav.audit'), icon: ScrollText }], [t]); const items = admin ? adminItems : userItems; useEffect(() => { const prefetch = () => { for (const item of items) { if (!pathname.startsWith(item.href)) { router.prefetch(item.href); } } }; const idle = (window as Window & { requestIdleCallback?: (cb: () => void) => number }).requestIdleCallback; if (idle) { const id = idle(prefetch); return () => { (window as Window & { cancelIdleCallback?: (id: number) => void }).cancelIdleCallback?.(id); }; } const timeout = window.setTimeout(prefetch, 50); return () => window.clearTimeout(timeout); }, [items, pathname, router]); return (<nav className='sticky bottom-0 z-20 border-t border-[var(--border-soft)] bg-white/92 px-3 py-2.5 backdrop-blur'><ul className='grid gap-2' style={{ gridTemplateColumns: 'repeat(' + items.length + ', minmax(0, 1fr))' }}>{items.map((item) => { const active = pathname.startsWith(item.href); const Icon = item.icon; return (<li key={item.href}><button type='button' onMouseEnter={() => router.prefetch(item.href)} onTouchStart={() => router.prefetch(item.href)} onClick={() => { if (!active) router.push(item.href); }} className={'touch-manipulation flex min-h-[56px] w-full flex-col items-center justify-center gap-1 rounded-[13px] px-2 py-1.5 text-xs transition active:scale-[0.98] ' + (active ? 'bg-gradient-to-r from-[#43d8ff1f] via-[#4f7bff2b] to-[#d946ef21] text-blue-700' : 'text-slate-500 hover:bg-[rgba(210,223,255,0.45)]')} aria-current={active ? 'page' : undefined}><Icon className='h-4 w-4' />{item.label}</button></li>); })}</ul></nav>); }
